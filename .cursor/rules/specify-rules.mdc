# pick-a-zoo Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-18

## Active Technologies
- Python 3.12 + textual (TUI framework), rich (rendering engine), PyYAML (configuration file handling), pydantic (data model validation), platformdirs (cross-platform config paths), loguru (structured logging) (003-view-saved-cams)
- YAML configuration file (feeds.yaml) stored in platform-appropriate user data directory via platformdirs (read-only in this feature) (003-view-saved-cams)
- Python 3.12 + PyQt6 (GUI framework for video windows), ffpyplayer (video playback engine supporting HLS, RTSP, MP4, WebM), PyYAML (configuration file handling), pydantic (data model validation), platformdirs (cross-platform config paths), loguru (structured logging) (004-launch-video-window)
- YAML configuration file (feeds.yaml) stored in platform-appropriate user data directory via platformdirs (read and write for window dimensions) (004-launch-video-window)
- Python 3.12 + PyQt6 (GUI framework for video window button), ffpyplayer (video playback and frame extraction - existing), imageio-ffmpeg or opencv-python (video encoding for timelapse creation - NEEDS CLARIFICATION), numpy (frame data manipulation - existing), pillow (image processing if needed - existing), platformdirs (cross-platform directory paths - existing), loguru (structured logging - existing) (005-create-timelapse)
- File system (timelapse videos saved to `timelapses/` directory in application data directory, created automatically if missing) (005-create-timelapse)

- Python 3.12 + httpx (HTTP client), beautifulsoup4 (HTML parsing), lxml (HTML parser backend), m3u8 (HLS playlist parsing), PyYAML (configuration file handling), pydantic (data model validation), textual (TUI framework), rich (rendering engine), platformdirs (cross-platform config paths), loguru (structured logging) (002-add-cam-feed)

## Project Structure

```text
src/
tests/
```

## Commands

cd src; pytest; ruff check .

## Code Style

Python 3.12: Follow standard conventions

## Recent Changes
- 005-create-timelapse: Added Python 3.12 + PyQt6 (GUI framework for video window button), ffpyplayer (video playback and frame extraction - existing), imageio-ffmpeg or opencv-python (video encoding for timelapse creation - NEEDS CLARIFICATION), numpy (frame data manipulation - existing), pillow (image processing if needed - existing), platformdirs (cross-platform directory paths - existing), loguru (structured logging - existing)
- 004-launch-video-window: Added Python 3.12 + PyQt6 (GUI framework for video windows), ffpyplayer (video playback engine supporting HLS, RTSP, MP4, WebM), PyYAML (configuration file handling), pydantic (data model validation), platformdirs (cross-platform config paths), loguru (structured logging)
- 003-view-saved-cams: Added Python 3.12 + textual (TUI framework), rich (rendering engine), PyYAML (configuration file handling), pydantic (data model validation), platformdirs (cross-platform config paths), loguru (structured logging)


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
